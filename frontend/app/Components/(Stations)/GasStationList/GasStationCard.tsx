import React from 'react'
import Card from '../../Card/Card'
import styles from './styles.module.css'

type Props = {
}

const GasStationCard: React.FC<Props> = () => {
    return (
        <Card>
            <div className={styles.gasStationCardCont}>dfafas</div>
        </Card>
    )
}

export default GasStationCard